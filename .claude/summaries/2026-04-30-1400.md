# Autonomous Run -- 2026-04-30

## Task
Build the dedicated Drawings tab -- move floor plan drawings from the Structure tab to a new Drawings tab on the job detail page.

## Completed
- 72295fb: Add dedicated Drawings tab, move floor plan drawings from Structure tab

## What was done
1. **Created `drawings-tab.tsx`** -- New dedicated Drawings tab component:
   - Fetches buildings, level drawings, and penetration pins in parallel
   - Organized by building > level with collapsible accordion
   - Drawing thumbnails in a grid with click-to-preview
   - Full FloorPlanViewer with pins when a drawing is selected for preview
   - Upload button per level (admin/manager only)
   - Delete button per drawing (admin/manager only)
   - Summary bar showing total drawings and pinned penetrations
   - Auto-expands buildings that have drawings
   - Empty state directing users to Structure tab to add buildings first

2. **Updated `job-detail-view.tsx`**:
   - Added `drawings` to Tab union type
   - Added Drawings tab with Map icon (visible to all roles, not just admin)
   - Renders DrawingsTab component with jobId, companyId, userRole, userId props

3. **Cleaned up `building-structure.tsx`**:
   - Removed all drawing-related imports (level-drawings service, Upload/FileImage/X icons)
   - Removed drawing state variables (drawingsMap, drawingUrls, uploadingLevel, previewDrawing, fileInputRef, uploadLevelRef)
   - Removed drawing handler functions (triggerDrawingUpload, handleDrawingFileChange, handleDeleteDrawing)
   - Removed hidden file input element
   - Removed Floor Plans section from level content
   - Removed drawing preview lightbox
   - Removed drawing count badge from level headers
   - Structure tab now focuses purely on building/level/room CRUD

## Remaining
Nothing -- task complete.

## Blockers / Decisions for Oliver
None.

## SQL Migrations to Run
None -- no schema changes needed. Uses existing `level_drawings` and `penetrations` tables.

## Files Changed
- app/(dashboard)/jobs/[id]/drawings-tab.tsx (NEW)
- app/(dashboard)/jobs/[id]/job-detail-view.tsx (MODIFIED)
- app/(dashboard)/jobs/[id]/building-structure.tsx (MODIFIED)
